# Neon Postgres — copy to .env and fill in your connection string.
# Dashboard: Neon project → Connection details → pooled connection string
DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require
